# CLAUDE.md 开发准则

## 📋 项目信息
- **项目名称**：ImageBatch - 图片拼接工具
- **技术栈**：Vue 3 + TypeScript + Pinia + Vite + Canvas API
- **测试框架**：Jest + Playwright
- **代码风格**：TypeScript严格模式 + Vue 3 Composition API

## 🌐 语言使用强制规范（最高优先级）

⚠️ **绝对强制使用简体中文**：
- ✅ 所有 AI 回复、文档、注释、日志、提交信息
- ✅ 测试用例描述、错误提示、配置说明
- ✅ 所有 `.claude/` 目录下的文档

**唯一例外**：代码标识符（变量名、函数名、类名）遵循项目既有命名约定。

## 🎯 核心原则

### 1. 编码前强制上下文收集
- 至少分析 **3个** 相似实现，识别可复用组件
- 理解项目命名约定、代码风格、测试模式
- 生成上下文摘要文件：`.claude/context-summary-[任务名].md`
- **禁止**：在未完成上下文检索的情况下直接编码

### 2. 质量验证机制
- 所有改动必须提供本地验证步骤（测试、构建、手动验证）
- 失败时立即终止提交，记录原因到 `operations-log.md`
- 生成验证报告：`.claude/verification-report.md`

### 3. 架构与复用优先
- 优先复用既有组件和工具函数
- 禁止重复造轮子，必须先搜索项目内是否已有实现
- 遵循 SOLID 原则和 DRY 原则

## 🔒 验证与审查机制

### 本地验证要求
- 所有改动必须通过本地验证：`npm run test` + `npm run build`
- 验证失败时立即终止，记录到 `.claude/operations-log.md`
- 无法自动验证的部分，必须提供手动验证步骤

### 质量审查标准
生成验证报告到 `.claude/verification-report.md`，包含：

**技术维度评分（0-100）**：
- 代码质量：遵循 TypeScript 严格模式、Vue 3 最佳实践
- 测试覆盖：单元测试覆盖核心逻辑、E2E 测试覆盖关键流程
- 规范遵循：命名约定、注释规范、文件组织

**决策规则**：
- ≥90分 → 通过
- <80分 → 退回修改
- 80-89分 → 仔细审阅后决策

## ✅ 代码质量标准

### 📝 注释规范
- **编码**：UTF-8 无 BOM
- **语言**：所有注释必须使用简体中文
- **内容**：描述意图和约束，而非重复代码逻辑
- **禁止**：修改说明式注释（由 Git 记录）

### 🧪 测试要求
- **覆盖范围**：正常流程 + 边界条件 + 错误处理
- **测试类型**：
  - 单元测试（Jest）：核心逻辑函数
  - E2E 测试（Playwright）：关键用户流程
- **执行时机**：提交前必须通过 `npm run test`

### 🏗️ 设计原则
- **SOLID 原则**：单一职责、开闭原则、依赖倒置
- **DRY 原则**：共享逻辑必须抽象为可复用组件
- **关注点分离**：UI组件、业务逻辑、数据模型分离
- **Vue 3 最佳实践**：
  - 使用 Composition API
  - 响应式状态管理（Pinia）
  - 组件化设计，职责明确

### 💻 实现标准
- **完整实现**：禁止 MVP、占位符，必须完成全量功能
- **代码整洁**：删除过时、重复代码
- **类型安全**：TypeScript 严格模式，零编译错误
- **命名约定**：
  - 组件：PascalCase（如 `CanvasRenderer.vue`）
  - 函数/变量：camelCase（如 `calculateLayout`）
  - 常量：UPPER_SNAKE_CASE（如 `MAX_HISTORY_SIZE`）
  - 类型：PascalCase + `Type` 后缀（如 `LayoutConfigType`）

### ⚡ 性能意识
- **Canvas 渲染优化**：避免频繁重绘，使用 `requestAnimationFrame`
- **状态管理优化**：合理使用计算属性，避免不必要的响应式
- **图片处理优化**：缩略图缓存、懒加载

## 🚀 工作流程

### 📋 标准流程（6步骤）
1. **分析需求**：理解用户意图，识别关键疑问
2. **获取上下文**：搜索相似实现，理解项目模式
3. **任务规划**：拆解任务，定义验收标准
4. **编码实现**：遵循代码规范，增量提交
5. **本地验证**：运行测试和构建

### 🔍 工具使用规范

#### 文件操作（优先使用 Claude Code 内置工具）
- **搜索文件**：`Glob` 工具（如 `**/*.vue`）
- **搜索内容**：`Grep` 工具（支持正则表达式）
- **读取文件**：`Read` 工具
- **编辑文件**：`Edit` 工具（精确替换）
- **写入文件**：`Write` 工具（新建或覆盖）

#### 信息检索（按需使用）
- **Vue 3 文档**：优先使用 WebFetch 访问官方文档
- **TypeScript 文档**：优先使用 WebFetch 访问官方文档
- **开源实现示例**：使用 WebSearch 搜索 GitHub
- **问题排查**：使用 WebSearch 搜索 Stack Overflow

### 🔍 编码前上下文收集（强制执行）

**核心原则**：研究先于编码，复用优于创造。

#### 任务复杂度分级
- **简单任务**（单文件、<50行）：执行步骤 1-3
- **中等任务**（多文件、<200行）：执行步骤 1-5
- **复杂任务**（架构级、>200行）：执行全部 7 步骤

#### 检索清单

**步骤1：搜索相似文件**
```bash
Glob pattern="**/*{关键词}*.{vue,ts}"
```
目标：找到 3-5 个相似实现

**步骤2：搜索关键代码**
```bash
Grep pattern="函数名|类名" output_mode="content" -n
```
目标：定位关键实现位置

**步骤3：深度阅读代码**
```bash
Read file_path  # 至少读取3个相关文件
```
关注点：
- 实现模式（组件化、状态管理）
- 可复用函数/类
- 命名约定和代码风格

**步骤4：查找可复用组件**（中等任务及以上）
```bash
Glob pattern="src/{utils,helpers,core}/**/*.ts"
```
目标：避免重复造轮子

**步骤5：分析测试代码**（中等任务及以上）
```bash
Grep pattern="describe|it|test" glob="*.spec.ts"
```
目标：理解测试模式

**步骤6：查询官方文档**（复杂任务）
- Vue 3 文档：https://vuejs.org/
- Pinia 文档：https://pinia.vuejs.org/
- TypeScript 文档：https://www.typescriptlang.org/

**步骤7：搜索开源实现**（复杂任务）
```bash
WebSearch query="Vue 3 Canvas 实现示例"
```
目标：学习最佳实践

#### 充分性验证（编码前必答）

必须能回答以下问题：

1. ✅ 找到了至少 3 个相似实现？
2. ✅ 理解了项目的实现模式？
3. ✅ 找到了可复用的组件/函数？
4. ✅ 理解了命名约定和代码风格？
5. ✅ 知道如何测试这个功能？

**无法全部回答"是" → 禁止编码，继续收集上下文。**

### 📄 上下文摘要文件（编码前必须生成）

**路径**：`.claude/context-summary-[任务名].md`

**必须包含内容**：
1. **相似实现分析**：至少 3 个，包含文件路径和行号
2. **项目约定**：命名规范、文件组织、代码风格
3. **可复用组件**：可直接使用的函数/类/工具
4. **测试策略**：参考文件、覆盖要求
5. **关键风险点**：边界条件、性能、依赖

### 🔄 编码规范检查（防止懒惰）

**编码前检查**（记录到 `.claude/operations-log.md`）：
- ✅ 已查阅上下文摘要文件
- ✅ 找到可复用组件：[列出路径]
- ✅ 理解命名约定和代码风格
- ✅ 确认没有重复造轮子

**编码后验证**（记录到 `.claude/operations-log.md`）：
- ✅ 复用了哪些既有组件
- ✅ 遵循了项目约定（举例证明）
- ✅ 对比了相似实现（说明差异和理由）
- ✅ 通过了本地测试和构建

## 📋 文件结构

所有工作文件写入 `.claude/` 目录：
```
.claude/
├── context-summary-[任务名].md   ← 上下文摘要
├── operations-log.md             ← 操作日志
└── verification-report.md        ← 验证报告
```

## 💡 开发哲学

### 核心原则
- **渐进式迭代**：小步提交，保持可编译、可验证
- **研究先于编码**：理解既有实现，吸收项目经验
- **务实态度**：满足真实需求，避免过度设计
- **清晰表达**：拒绝炫技，以可读性为先
- **简单方案**：避免过早优化和过度架构

### 简单性定义
- 单一职责：每个函数/类只做一件事
- 延迟抽象：重复 3 次以上再考虑通用化
- 可读优先：如果需要解释，说明太复杂
- 遵循约定：沿用项目既有风格

## 🔧 项目特定规则

### ImageBatch 项目约定
- **组件化**：UI 组件独立，职责明确
- **状态管理**：使用 Pinia，避免 prop drilling
- **Canvas 渲染**：封装到独立模块，避免污染组件
- **测试覆盖**：核心逻辑必须有单元测试
- **类型安全**：TypeScript 严格模式，零 `any`

### 禁止事项
- ❌ 在缺乏证据时做假设
- ❌ 跳过上下文收集直接编码
- ❌ 重复造轮子
- ❌ 编写 MVP 或占位符代码
- ❌ 违反项目命名约定和代码风格

### 必须做到
- ✅ 编码前生成上下文摘要
- ✅ 复用既有组件和工具函数
- ✅ 编写测试并通过验证
- ✅ 记录操作日志和验证报告
- ✅ 连续三次失败后暂停复盘

## 📚 参考文档

- **功能设计**：`功能提示词.md`
- **项目结构**：`src/` 目录
- **测试示例**：`tests/` 目录
- **操作记录**：`.claude/operations-log.md`

---

**文档版本**：v2.0
**更新时间**：2025-10-16
**适用项目**：ImageBatch - 图片拼接工具