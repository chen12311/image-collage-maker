# ImageBatch - 在线图片拼接工具 - Requirements Document

一个简洁高效的在线图片拼接工具，支持多图横向/纵向拼接、布局自定义、文字添加和一键导出。采用统一网格引擎设计，消除特殊情况处理，确保代码简洁和用户体验流畅。

## Core Features

### 核心数据结构定义 (Data Structures First - Linus原则)
```typescript
// 统一元素模型 - 消除图片/文字特殊情况
interface Element {
  id: string
  type: 'image' | 'text'
  content: ImageData | TextData
  transform: {
    x: number, y: number     // 网格坐标，非像素坐标
    width: number, height: number  // 网格单元数
    rotation: number         // 弧度
  }
}

// 布局引擎 - 统一所有拼接模式
interface LayoutEngine {
  grid: { rows: number, cols: number }
  elements: Element[]
  spacing: number          // 统一间距
  canvas: { width: number, height: number }
}
```

### 图片拼接引擎
- **文件约束**：单文件≤50MB，总计≤500MB，格式限JPG/PNG/WebP
- **布局统一**：横向=1×N网格，纵向=N×1网格，网格=M×N网格
- **渲染管道**：文件解析→网格计算→Canvas渲染→导出压缩

### 布局控制系统
- **间距调节**：统一的padding/margin控制
- **边框样式**：宽度、颜色、圆角设置
- **预设尺寸**：常用社交媒体比例（1:1、16:9、9:16等）

### 内容编辑
- **文字添加**：支持自定义字体、大小、颜色
- **背景设置**：纯色填充或图案背景

### 导出功能
- **一键下载**：生成最终拼接图片
- **格式选择**：PNG、JPG格式输出

## User Stories

### 主要用例场景 (Real Problems, Not Imaginary Ones)

**1. 教程创作者 - 小红书/Instagram Story**
- 输入：3-6张截图(1080×1920)，总大小约15MB
- 需求：纵向拼接，间距20px，导出9:16比例
- 频率：每日3-5次，单次耗时需<2分钟
- 成功标准：一键操作，无需调整尺寸

**2. 电商产品对比 - 淘宝详情页**  
- 输入：8-12张产品图(800×800)，总大小约40MB
- 需求：3×4网格布局，白色背景，统一边框
- 频率：每周1-2次，批量处理
- 成功标准：保持图片清晰度，支持高分辨率导出

**3. 设计作品集 - Behance/Dribbble**
- 输入：4-8张设计稿(1200×900)，总大小约60MB  
- 需求：2×4网格，精确间距控制，保持原始比例
- 频率：每月2-3次
- 成功标准：像素级精确控制，专业视觉效果

**4. 移动端快速拼图 - 微信朋友圈**
- 输入：3-9张手机照片(3024×4032)，总大小约80MB
- 需求：自动压缩，方形输出，触摸操作
- 频率：随时随地使用
- 成功标准：30秒内完成，网络环境下流畅运行

## Acceptance Criteria

### 边界情况与错误处理 (Edge Cases - "代码必须处理现实")

**文件处理边界**
- [ ] 单图超过50MB：自动压缩至合理大小，显示压缩比例
- [ ] 总容量超过500MB：拒绝新增，提示删除部分图片
- [ ] 非图片文件：友好错误提示，不中断其他文件处理
- [ ] 损坏图片文件：跳过处理，记录错误日志

**内存管理边界**  
- [ ] 设备内存不足：自动降低图片分辨率，优先保证功能可用
- [ ] 浏览器崩溃边缘：实时监控内存使用，主动释放缓存
- [ ] 大图渲染超时：分片渲染，显示进度条

**网络环境边界**
- [ ] 离线状态：所有核心功能正常工作
- [ ] 慢网络：静态资源本地缓存，无网络依赖功能
- [ ] 网络中断：不影响已加载图片的处理

**设备兼容边界**
- [ ] 低端设备：自动调整处理策略，降低图片质量但保证速度
- [ ] 屏幕分辨率：UI自适应，小屏设备简化操作界面
- [ ] 触摸设备：手势操作替代精确点击

### 核心功能验收 (Core Functions)
- [ ] 批量上传：支持拖拽、点击、粘贴三种方式，最多20张图片
- [ ] 统一网格：横向(1×N)、纵向(N×1)、网格(M×N)，零特殊情况代码
- [ ] 实时预览：拖拽排序、即时生效，60FPS流畅度
- [ ] 撤销重做：支持Ctrl+Z/Ctrl+Y，最多保存10步操作历史

### 性能验收 (Performance - Linus标准: 实际可用)
- [ ] 首次加载时间 < 3秒（移动网络）
- [ ] 10张1MB图片拼接处理时间 < 5秒
- [ ] 导出2000px宽度图片时间 < 3秒
- [ ] 内存使用不超过设备可用内存的50%

### 用户体验验收 (UX - 零学习成本)
- [ ] 新用户无需教程即可完成首次拼接
- [ ] 所有操作可在3次点击内完成
- [ ] 支持Ctrl+Z撤销操作
- [ ] 移动端触摸操作流畅，无卡顿

### 兼容性验收 (Compatibility - Never break userspace)
- [ ] 支持Chrome/Safari/Firefox最新2个版本
- [ ] 支持iOS Safari和Android Chrome
- [ ] 支持JPG、PNG、WebP格式图片
- [ ] 导出图片在所有主流图片查看器中正常显示

## Non-functional Requirements

### 性能要求 (Performance - "Premature optimization is evil, but so is premature pessimization")
- **Canvas渲染优化**：使用OffscreenCanvas进行后台渲染，避免阻塞UI线程
- **内存管理**：大图片自动压缩，避免内存溢出崩溃
- **渐进式加载**：图片按需加载和渲染，不一次性加载所有内容
- **缓存策略**：合理使用浏览器缓存，避免重复计算

### 安全要求 (Security - "Security is a process, not a product")
- **客户端处理**：所有图片处理在浏览器本地进行，不上传到服务器
- **文件类型验证**：严格检查上传文件类型，防止恶意文件
- **内容安全策略**：实施CSP防止XSS攻击
- **HTTPS传输**：所有网络请求必须使用HTTPS

### 可维护性要求 (Maintainability - "Good code documents itself")
- **模块化设计**：图片处理、布局引擎、UI组件完全解耦
- **零第三方依赖**：除Vue.js外不引入其他重度依赖，保持代码可控
- **类型安全**：使用TypeScript确保类型安全
- **测试覆盖**：核心算法必须有单元测试

### 可扩展性要求 (Scalability - "Plan for success")
- **插件架构**：布局引擎支持新增布局模式而无需修改核心代码
- **配置驱动**：所有预设尺寸、样式通过配置文件定义
- **国际化准备**：文案与逻辑分离，支持多语言扩展

## 详细用户操作流程 (User Interaction Flow)

### 标准操作路径 (Happy Path - 3次点击原则)
```
1. 首次访问
   └─ 拖拽图片到页面 OR 点击上传按钮
   └─ 自动检测最佳布局（纵向/横向/网格）
   └─ 实时预览生成
   └─ 点击下载按钮
   
2. 高级定制 (可选)
   └─ 调整间距滑块 (0-50px)
   └─ 选择输出尺寸 (预设或自定义)
   └─ 添加文字标签 (可选)
   └─ 调整背景色 (默认白色)
```

### 移动端交互适配
```
1. 触摸上传：点击大面积上传区域
2. 图片排序：长按拖拽重新排序  
3. 参数调节：滑动手势调整间距
4. 预览缩放：双指缩放查看细节
5. 一键分享：导出后直接分享到社交媒体
```

### 错误恢复流程
```
1. 文件错误：显示错误图片，允许替换或删除
2. 内存不足：自动降级处理，提示用户选择
3. 处理超时：显示进度，允许取消操作
4. 网络问题：离线模式提示，功能不受影响
```

## 技术约束与限制 (Technical Constraints)

### 浏览器API限制
- **Canvas最大尺寸**：受浏览器限制，通常32767×32767px
- **内存使用**：单次处理不超过设备可用内存的50%
- **文件处理**：使用FileReader API，支持现代浏览器
- **OffscreenCanvas**：Chrome/Edge 69+，Safari 16.4+，Firefox 105+

### 性能基准数据 (Performance Benchmarks)
```
设备类型          | 图片数量 | 分辨率      | 处理时间  | 内存占用
低端手机(2GB RAM) | 5张     | 1080×1920  | <8秒     | <300MB
中端手机(4GB RAM) | 10张    | 1080×1920  | <5秒     | <500MB  
高端手机(8GB RAM) | 15张    | 3024×4032  | <5秒     | <800MB
桌面浏览器        | 20张    | 4000×3000  | <3秒     | <1GB
```

### 兼容性矩阵 (Compatibility Matrix)
| 浏览器     | 版本要求 | Canvas支持 | OffscreenCanvas | 文件API |
|------------|----------|------------|-----------------|---------|
| Chrome     | 90+      | ✅         | ✅              | ✅      |
| Safari     | 15+      | ✅         | ✅ (16.4+)      | ✅      |
| Firefox    | 88+      | ✅         | ✅ (105+)       | ✅      |
| Edge       | 90+      | ✅         | ✅              | ✅      |
| iOS Safari | 15+      | ✅         | ❌ (降级处理)    | ✅      |
| Android    | 90+      | ✅         | ✅              | ✅      |
